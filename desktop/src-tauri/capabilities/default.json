{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "T101 面板默认权限：窗口控制 + 自定义命令（数据全部内嵌，无远程 URL）",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-start-dragging"
  ]
}
