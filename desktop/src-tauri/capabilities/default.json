{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "T101 面板默认权限：窗口控制 + 自定义命令",
  "windows": ["main"],
  "remote": {
    "urls": ["http://127.0.0.1:8765/*"]
  },
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-hide",
    "core:window:allow-minimize",
    "core:window:allow-start-dragging"
  ]
}
